
{{!-- creates the post cards --}}
  {{#each posts as |Post| }}
  <div class="card">
    <div class="card-header">
      {{Post.title}}
    </div>
    <div class="card-body">
      <p class="card-text">{{Post.description}}</p>
      <p>{{Post.writer_id}}</p>
    </div>
  </div>
  {{/each}}
